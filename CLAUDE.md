<!-- COMMON_RULE START -->
# 공통 AI 에이전트 운영 룰

이 repo의 AI 작업은 **모든 프로젝트 공통 운영 룰**(정본·단일 소스)을 따릅니다:

- 정본: `/Users/basil/Projects/project-manager/ai_agent_rule.md`

핵심 요약(자세한 건 정본):
- **작업 크기에 맞춰 절차 조절** — Trivial/Small은 fast-path(문서 통독·커밋상태 하네스·핸드오프 게이트 생략/축소), 큰 작업만 풀코스.
- **낭비 제거** — 커밋상태 스탬프는 세션/PR 끝 1회 + 바뀐 문서만(`--file`), 검증은 슬라이스마다가 아니라 커밋 직전 1회.
- **병렬** — 문서 갱신은 서로 다른 파일로 팬아웃, 빌드는 백그라운드, git 커밋 꼬리는 직렬.
- 커밋/푸시는 사용자가 요청할 때만.

--- 아래는 이 repo 고유 규칙 ---
<!-- COMMON_RULE END -->

# 프로젝트 문서

모든 프로젝트 문서는 [docs/README.md](docs/README.md)를 참고하세요.

# 개발 워크플로우

1. **스펙 작성** → `SPEC.md`에 기능 요구사항 정의
2. **구현** → 스펙 기반으로 코드 작성
3. **정책 문서 업데이트** → 구현 완료 후 `docs/product/policies/` 해당 문서 동기화
4. **의사결정(ADR)** → 구조, 정책, 기술 선택 등 의사결정이 발생하면 `docs/decisions/ADR.md`에 기록
   - 형식: `## YYYY-MM-DD | 결정 제목` + 결정/이유/대안
   - 사소한 변경(오타, 스타일)은 제외. 왜 그렇게 했는지 나중에 궁금할 만한 것만 기록
5. **커밋 & 로컬 확인** → 구현 작업이 끝나면 커밋하고 로컬에서 동작을 확인한다 (별도 요청 불필요)
   - **푸시는 자동으로 하지 않는다.** 사용자가 직접/명시적으로 요청할 때만 `git push`
   - 이유: Vercel이 push마다 빌드를 돌리는데 빌드 횟수에 제한이 있어, 여러 커밋을 모아 한 번에 푸시한다
